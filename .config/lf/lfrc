map D delete
map R reload

# for win32 with busybox and lf-shell installed:
#   busybox-w32: https://frippery.org/busybox/
#   lf-shell: https://github.com/lxhillwind/utils/blob/main/lf-shell.zig
#
#   (leading : is to make block copy&paste work)
#:set shell lf-shell
#:set shellflag -c
#:cmd doc $lf -doc | less
#:cmd open &cmd /s /c start '' "$F"
#:map e $vi "$F"
#:map i $less "$F"
#:map w $lf-shell
