#!/bin/sh

if [ -e /usr/bin/pbpaste ]; then
    exec /usr/bin/pbpaste "$@"
else
    if [ -n "$WAYLAND_DISPLAY" ]; then
        exec wl-paste -n
    elif [ -n "$DISPLAY" ]; then
        exec xsel -ob
    elif [ -n "$TMUX" ]; then
        exec tmux saveb -
    else
        :
    fi
fi
