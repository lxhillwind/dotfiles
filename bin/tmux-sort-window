#!/bin/bash
set -e

if [ -z "$TMUX" ]; then
    echo 'not in tmux session.' >&2
    exit 1
fi

tmux movew -r

# move current window to index 3 (which should exist):
#   movew -b -t 3
#   (-b means before)
